```
version: '3.8'
services:
  db:
    image: mariadb:10.11
    container_name: db
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: P@ssw0rd
      DB_NAME: testdb
      DB_USER: test
      DB_PASSW: P@ssw0rd
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - testapp
  testapp:
    image: site:latest
    container_name: testapp
    restart: always
    ports:
      - "8080:8000"
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_NAME: testdb
      DB_USER: test
      DB_PASS: P@ssw0rd
      DB_TYPE: maria
    networks:
      - testapp
volumes:
  db_data:
networks:
  testapp:
    driver: bridge
```
